<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Upload Json</title>
</head>
<body>
    <div class="container-lg">
        <div>
            <h2>Upload a file containing strings to be converted to json</h2>
            <form action="/show" method="POST" enctype="multipart/form-data">
                <input type="file" name="file" id="fileInput" multiple onchange="showname()" required/>
                <input class="btn btn-primary" type="submit" value="Upload" />
            </form>
        </div>
    </div>
    <script>
        function showname(){
            // copy the name of file
            let name = document.getElementById('fileInput');
            navigator.clipboard.writeText(name.files.item(0).name);
            alert('Selected file: ' + name.files.item(0).name);
        }
    </script>
</body>
</html>